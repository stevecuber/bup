<%= render 'submenu' %>
<div id='records'>
  <% if searching? %>
    <%= link_to t('cancel_search', :scope => 'refinery.admin.search'), refinery.careers_admin_careers_path, :class => "cancel-search" %>
    <h2><%= t('results_for_html', :scope => 'refinery.admin.search', :query => h(params[:search])).html_safe %></h2>
    <% if @careers.any? %>
      <ul>
        <%= render :partial => 'career', :collection => @careers %>
      </ul>
    <% else %>
      <p><%= t('no_results', :scope => 'refinery.admin.search') %></p>
    <% end %>
  <% else %>
    <% if @grouped_careers.any? -%>
      <% @grouped_careers.each do |container| %>
        <h3><%= l((career_group = container.last).first.created_at) %></h3>
        <ul>
          <%= render :partial => 'career', :collection => career_group %>
        </ul>
      <% end %>
    <% else -%>
      <p>
        <strong><%= t('.no_spam') %></strong>
      </p>
    <% end -%>
  <% end %>
</div>
